Steps to run the code : 
i) Run the command 'python3 answer.py' It will give the values of E, A and decrypted password. 

To know about how we fetched ciphertext from plaintext, run the following commands :   
ii) Run command.cpp. It will create commands.txt. 
iii)Now run the command "ssh -i id_rsa student@65.0.124.36 < commands.txt > output.txt". Stop it after 2 minutes using Ctrl-C, if it did not stop automatically. It will generate output.txt. 
iv) Now run clean.cpp. You will get the cipher.txt
